String data = """# NOTE \ SwiftUI中的数据流控制（一）各种State
@[toc]
SwiftUI会根据View(视图)中包含数据的变化自动刷新，因此其设计了多种Property Wrapper（属性包装器）来实现不同效果的状态控制。
@State @Binding @AppStorage @SceneStorage....

![在这里插入图片描述](https://img-blog.csdnimg.cn/20201206214322571.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dlaXhpbl80NTAzMjkzOA==,size_16,color_FFFFFF,t_70)

在SwiftUI中，用户的**行为**<sup>Action</sup>会改变UI的**状态**<sup>State</sup>，进而UI会更新与这种状态相关联的**视图界面**<sup>View</sup>。因此这种State起到控制视图变化的作用。
********
## @State
>*创建一个可以控制视图变化的**值类型**变量*
在SwiftUI中，用户的**行为**<sup>Action</sup>会改变UI的**状态**<sup>State</sup>，进而UI会更新与这种状态相关联的**视图界面**<sup>View</sup>。因此这种State起到控制视图变化的作用。
********
## @State
>*创建一个可以控制视图变化的**值类型**变量*
在SwiftUI中，用户的**行为**<sup>Action</sup>会改变UI的**状态**<sup>State</sup>，进而UI会更新与这种状态相关联的**视图界面**<sup>View</sup>。因此这种State起到控制视图变化的作用。
********
## @State
>*创建一个可以控制视图变化的**值类型**变量*
在SwiftUI中，用户的**行为**<sup>Action</sup>会改变UI的**状态**<sup>State</sup>，进而UI会更新与这种状态相关联的**视图界面**<sup>View</sup>。因此这种State起到控制视图变化的作用。
********
## @State
>*创建一个可以控制视图变化的**值类型**变量*""";
